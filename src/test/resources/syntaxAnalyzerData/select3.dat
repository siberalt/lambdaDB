SELECT column1, column2 + 2, (column3 - column2) + "some string" as col4
 FROM table2 t2
 INNER JOIN table1 ON t1.id = t2.product_id
 LEFT JOIN (SELECT * FROM users u WHERE u.id=10) ON 1
 ORDER BY col1